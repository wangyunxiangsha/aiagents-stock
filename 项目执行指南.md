# 项目执行指南

本文说明如何在本机或 Docker 中**安装依赖、配置环境、启动应用**。详细功能说明见根目录 `README.md`。

源码按功能放在 **`stockapp/`** 包下（`common`、`core`、`monitoring`、各业务子包等）；启动仍使用根目录的 **`app.py`** / **`run.py`**，命令与重构前一致。

---

## 一、环境要求

| 项目 | 说明 |
|------|------|
| Python | 3.8 及以上（README 推荐 3.12） |
| 操作系统 | Windows / macOS / Linux |
| 网络 | 需能访问行情接口与所配置的 AI API；大陆环境使用问财等时建议勿开全局 VPN |
| Node.js | **可选**；使用 pywencai（问财）相关功能时 README 要求 Node 16+ |

---

## 二、本地执行（推荐开发者）

### 1. 获取代码并进入目录

```powershell
cd d:\document\desktop\cursor-demo\aiagents-stock
```

（请将路径换成你本机上的项目根目录。）

### 2. 创建虚拟环境并安装依赖

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Linux / macOS：

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. 配置环境变量

将模板复制为 `.env` 并编辑（**至少填写 AI 的 API Key**）：

```powershell
Copy-Item .env.example .env
```

用编辑器打开 `.env`，必填项示例：

```env
DEEPSEEK_API_KEY=你的密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEFAULT_MODEL_NAME=deepseek-chat
```

说明：变量名虽为 `DEEPSEEK_*`，`DEEPSEEK_BASE_URL` 可改为任意 **OpenAI 兼容** 服务商地址；`DEFAULT_MODEL_NAME` 与对端支持的模型名一致即可。

### 4. 启动应用

**方式 A — 使用项目自带脚本（固定端口 8503）**

```powershell
.\venv\Scripts\Activate.ps1
python run.py
```

脚本会检查主要依赖与 `DEEPSEEK_API_KEY` 是否已配置，然后启动 Streamlit。

- 浏览器访问：**http://127.0.0.1:8503**
- 停止服务：在终端按 **Ctrl+C**

**方式 B — 直接使用 Streamlit（默认常用端口 8501）**

```powershell
.\venv\Scripts\Activate.ps1
streamlit run app.py
```

- 一般访问：**http://localhost:8501**（若未改端口）
- 指定端口示例：`streamlit run app.py --server.port 8501`

> 注意：`run.py` 使用 **8503**，README 中 Docker 示例也映射到 **8503**；直接用 `streamlit run app.py` 时多为 **8501**，以终端输出为准。

### 5. Windows 快捷启动（可选）

若仓库中存在 `start_app.bat`，可在资源管理器中双击，或在命令行执行（需已配置好 venv 与 `.env`）。

---

## 三、Docker 执行

适合不想在本机安装 Python 依赖的场景。需已安装 **Docker** 与 **Docker Compose**。

### 1. 准备 `.env`

与本地相同：复制 `.env.example` 为 `.env`，至少配置 `DEEPSEEK_API_KEY`。

### 2. 启动

在项目根目录执行：

```bash
docker-compose up -d
```

或按 README 使用 `docker build` / `docker run`，并将宿主机的 `.env` 挂载进容器（README 中有示例命令）。

### 3. 访问

按 `docker-compose.yml` 或 `docker run -p` 映射访问，常见为：

- **http://localhost:8503**（与 README 中端口映射一致时）

查看日志：

```bash
docker-compose logs -f
```

停止：

```bash
docker-compose down
```

国内构建慢时，可使用 README 提到的 **Dockerfile 国内源版** 构建镜像（见 `docs/DOCKER_CN_BUILD_GUIDE.md`）。

---

## 四、可选能力（按需配置）

以下不配也能启动主界面；对应功能会不可用或降级。

| 配置项 | 用途 |
|--------|------|
| `TUSHARE_TOKEN` | 部分数据降级/补充 |
| `TDX_ENABLED` + `TDX_BASE_URL` | 本地通达信 HTTP 行情（AI 盯盘、监测等） |
| `MINIQMT_*` | 量化自动交易（需本机 miniQMT 客户端） |
| `EMAIL_*` / `WEBHOOK_*` | 邮件、钉钉/飞书通知 |

具体键名以 `.env.example` 为准；部分选项也可在应用内 **「环境配置」** 页面修改。

---

## 五、常见问题

1. **启动后无法分析股票**  
   检查 `.env` 中 API Key 是否有效、余额是否充足，`DEEPSEEK_BASE_URL` 是否与厂商文档一致。

2. **`python run.py` 提示缺少依赖**  
   确认已激活虚拟环境并执行 `pip install -r requirements.txt`。

3. **问财 / pywencai 报错**  
   安装 Node.js 16+；检查网络；关闭可能干扰的请求代理。

4. **端口被占用**  
   换用其他端口：`streamlit run app.py --server.port 新端口`。

---

## 六、相关文档

| 文档 | 内容 |
|------|------|
| `README.md` | 完整功能、架构、故障排除 |
| `docs/QUICK_START.md` | 快速开始 |
| `docs/DOCKER_DEPLOYMENT.md` | Docker 部署详解 |
| `分析.md` | 项目结构与模块说明（若仓库中有） |

---

*执行方式以当前仓库代码为准；若上游更新启动命令或端口，请以 `run.py`、`docker-compose.yml` 及 README 为准。*
