# aiagents-stock 项目整理与分析

> 分析日期：2026-05-11  
> 说明：本文档基于仓库代码与 `README.md` 梳理，用于快速理解他人项目的结构、能力与运行方式。

---

## 1. 项目定位

**复合多 AI 智能体股票团队分析系统**：面向 A 股为主（兼港股、美股）的 **Streamlit Web 应用**，用多角色 LLM 模拟分析师团队，结合行情与资金数据，输出单股/批量/板块/龙虎榜/宏观等多类分析，并叠加 **实时价格监测、持仓定时分析、新闻流量监测、AI 盯盘（含 miniQMT 交易接口）** 等自动化能力。

- **初心与边界**：作者定位为辅助决策与学习工具；README 明确 **不构成投资建议**，需自备 OpenAI 兼容 API（如 DeepSeek）。
- **许可证**：MIT。

---

## 2. 技术栈一览

| 类别 | 技术 |
|------|------|
| 前端 / 应用壳 | Streamlit（宽布局、大量自定义 CSS） |
| 数据与计算 | pandas、numpy、akshare、yfinance、tushare（降级）、通达信 TDX HTTP API（可选） |
| AI 调用 | `openai` SDK + `.env` 中的 `DEEPSEEK_BASE_URL` / `DEFAULT_MODEL_NAME`（任意 OpenAI 兼容端点） |
| 问财 / 舆情相关 | pywencai、jieba、BeautifulSoup、lxml |
| 可视化 | Plotly |
| 报告 | ReportLab（PDF）、多处 Markdown/HTML 导出 |
| 持久化 | SQLite（监测、龙虎榜、新闻流、智策、持仓等多套 `*_db.py`）、Peewee（`requirements.txt`） |
| 调度 | `schedule`（智策定时、持仓定时、新闻调度等） |
| 容器 | Dockerfile、`docker-compose.yml`、国内源版 Dockerfile |

**运行入口**：`streamlit run app.py` 或 `python run.py`（见 README）。

---

## 3. 功能模块与代码对应（侧边栏导航）

主入口 **`app.py`**：侧边栏按钮切换「页面状态」，再 `from stockapp.<子包>... import` 各业务 UI。

业务代码集中在 **`stockapp/`** 包：`common`（公共库）、`core`（单股分析）、`monitoring`（实时监测 + 通知）、`main_force`、`sector_strategy`、`longhubang`、`news_flow`、`macro_analysis`、`macro_cycle`、`portfolio`、`smart_monitor`、`strategies`（多类选股策略）。

| 侧边栏功能 | 典型路径（`stockapp/` 下，非穷尽） |
|------------|-------------------------------------|
| 股票分析（首页） | `core/stock_data.py`、`core/ai_agents.py`、`core/pdf_generator.py`、`common/database.py` |
| 主力选股 | `main_force/main_force_*.py` |
| 低价擒牛 / 小市值 / 净利增长 / 低估值 | `strategies/low_price_bull_*.py`、`strategies/small_cap_*.py`、`strategies/profit_growth_*.py`、`strategies/value_stock_*.py` |
| 智策板块 | `sector_strategy/sector_strategy_*.py` |
| 智瞰龙虎 | `longhubang/longhubang_*.py` |
| 新闻流量 | `news_flow/news_flow_*.py`，单股新闻辅助在 `core/qstock_news_data.py` |
| 宏观分析 | `macro_analysis/macro_analysis_*.py` |
| 宏观周期 | `macro_cycle/macro_cycle_*.py` |
| 持仓分析 | `portfolio/portfolio_*.py` |
| AI 盯盘 | `smart_monitor/smart_monitor_*.py` |
| 实时监测 | `monitoring/monitor_*.py`、`monitoring/notification_service.py` |
| 历史记录 | `common/database.py` + `app.py` 内历史视图 |
| 环境配置 | 根目录 `config.py` + `common/config_manager.py`、`common/model_config.py`；`update_env_example.py` 仍在根目录 |

**横切能力**：`monitoring/notification_service.py`（邮件 + Webhook）、`common/data_source_manager.py`、`common/deepseek_client.py`、`core/risk_data_fetcher.py`。

---

## 4. 架构特点（简图）

```
用户浏览器
    ↓
Streamlit (app.py)
    ↓
┌───────────────┬────────────────┬─────────────────┐
│ 数据采集层     │ 多智能体分析层  │ 执行与通知层     │
│ stock_data    │ ai_agents      │ monitor_service │
│ akshare/yfin  │ sector_*_agents│ portfolio_sched │
│ TDX HTTP      │ longhubang_*  │ news_flow_sched  │
│ pywencai      │ macro_*_agents │ miniqmt_interface│
└───────────────┴────────────────┴─────────────────┘
    ↓                    ↓                ↓
SQLite / 文件缓存    OpenAI 兼容 API    邮件 / Webhook / 界面
```

- **模式**：业务代码已收拢到 **`stockapp/`** 按子包划分；根目录仅保留入口与配置。新功能仍在对应子包内 **新增 `*_ui.py` / `*_engine.py` / `*_db.py`** 即可。
- **配置**：`config.py` 统一从环境变量读取；敏感信息走 `.env`（模板为 `.env.example`）。

---

## 5. 目录与阅读顺序（认知地图）

根目录以 **`app.py` / `run.py` / `config.py`** 与 **`stockapp/`** 为主；`docs/` 为中文功能说明与部署文档。建议新人：

1. **`README.md`**：功能清单、环境变量、Docker/本地启动。  
2. **`requirements.txt`**、**`config.py`**。  
3. **`app.py`**：侧边栏 `st.button` 即全站导航。  
4. **`stockapp/<子包>/`**：按功能进入，优先读 `*_engine.py`、`*_agents.py`、`*_data.py`。

一次性迁移脚本（已执行）保存在 **`scripts/reorganize_to_stockapp.py`**，仅作记录；勿重复执行以免路径错乱。

---

## 6. 外部依赖与运行注意

- **必需**：Python 3.8+（README 推荐 3.12）、网络、**OpenAI 兼容 API Key**（变量名仍以 `DEEPSEEK_*` 为主，实际可指向其他厂商端点）。  
- **可选增强**：Node.js（README 称 pywencai 需要）、Tushare Token、TDX 自建 API、miniQMT 客户端、邮件与 Webhook。  
- **数据合规**：龙虎榜等接口有第三方额度与条款；问财链路依赖 pywencai，环境差异可能导致不稳定，项目内已有「数据源冗余」相关文档。

---

## 7. 文档资源索引

`docs/` 下已有较完整的使用与部署说明，包括但不限于：

- 部署：`DOCKER_DEPLOYMENT.md`、`DOCKER_CN_BUILD_GUIDE.md`、`QUICK_START.md`  
- 功能：`智策功能总览.md`、`智瞰龙虎功能说明.md`、`主力选股使用指南.md`、`新闻流量监测功能说明.md`、`宏观分析` 相关更新说明  
- 集成：`MINIQMT_INTEGRATION_GUIDE.md`、TDX 系列配置文档  
- 规范：`UNIFIED_ANALYSIS_SPEC.md`、`PORTFOLIO_USAGE.md`、`MULTI_SCHEDULE_GUIDE.md`  

`docs/AGENTS.md` 主要为 OpenSpec / AI 协作说明，与股票业务无直接关系。

---

## 8. 风险与可维护性简评

**优势**

- 功能覆盖面广，从单股深度分析到定时推送、量化接口一条链都有。  
- README 与 `docs` 对使用者友好。  
- 模型与部分基础设施可配置，便于换厂商 API。

**挑战**

- **`app.py` 体量极大**（数千行），侧边栏状态与 UI 耦合度高，后续改导航或抽离主题成本不低。  
- 模块命名中英文混用（longhubang、main_force 等），需熟悉业务词才能快速定位。  
- 多 SQLite 库与多调度器并存，部署时需留意 **单进程 Streamlit 下后台线程/定时任务** 与资源占用。

**安全提醒**：切勿将含真实 API Key、账号的 `.env` 提交仓库；生产环境应对外网访问与 miniQMT 权限做隔离。

---

## 9. 小结

该项目是一个 **以 Streamlit 为壳、多智能体 LLM 为脑、多数据源为血** 的个人/小团队向股票研究与辅助工具集，代码按业务垂直拆分清晰，但根目录文件数量多、主应用文件偏大。接手或二次开发时，建议 **先锁定侧边栏某一功能域，再沿 `ui → engine → agents/data → db` 路径阅读**，并优先复用 README 与 `docs` 中已有规范（如统一分析字段、批量分析模式等）。

---

*文档由仓库静态分析生成，未运行全量测试；若与上游仓库更新不一致，以实际代码为准。*
